import React from "react";
import PropTypes from "prop-types";
import Home from "@/src/views/Home";

const Page = (props) => {
  return (
    <div>
      <Home />
    </div>
  );
};

Page.propTypes = {};

export default Page;
